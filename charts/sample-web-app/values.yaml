image:
  repository: nginx
  tag: "stable"

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - /

resources: {}
  
nodeSelector: {}

tolerations: []

affinity: {}

env:
  NODE_ENV: stage
  PORT: "3000"